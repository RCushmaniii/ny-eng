[build]
  # Don't build the site - we only want the functions
  command = "echo 'Functions only build'"
  publish = "netlify-publish"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Environment variables are set in Netlify dashboard
# SUPABASE_URL
# SUPABASE_ANON_KEY
# RESEND_API_KEY
# NOTIFICATION_EMAIL
