---
// src/pages/index.astro
// This page's only job is to redirect from the root domain to the
// default language homepage (/en/). This is the best practice for SEO.
return Astro.redirect('/en/', 301);
---