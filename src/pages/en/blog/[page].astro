---
// DISABLED: This route conflicts with [slug].astro
// Use [...page].astro for pagination instead
export const prerender = true;

export async function getStaticPaths() {
  return [];
}
---

<!-- This page is disabled - pagination handled by [...page].astro -->
