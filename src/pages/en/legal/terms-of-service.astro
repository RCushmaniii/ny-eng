---
import Layout from '@layouts/Layout.astro';
import InnerHero from '@components/sections/InnerHero.astro';
import { getLegalContent } from '@utils/legalContent.js';

// Use the helper function to bypass TypeScript collection errors
const legalData = await getLegalContent('terms-of-service', 'en');
const { Content } = legalData;
const entry = legalData;

const heroContent = {
  title: entry.data.title,
  description: entry.data.seo?.description || "",
  overlayOpacity: 0.8
} as const;

const footerCta = {
  eyebrow: "",
  title: "Transform Your Professional English Today",
  description: "Book your free consultation to gain the clarity, confidence, and impact your role demands.",
  hideCta: true, // Hide the CTA for legal pages
  button: {
    text: "Reserve My Free Consultation",
    link: "https://ny-english-teacher.odoo.com/book/7ceefa20",
    target: "_blank"
  }
} as const;
---

<Layout 
  title={legalData.data.seo?.title || legalData.data.title}
  description={legalData.data.seo?.description || ''}
  lang="en"
  noIndex={false}
  footerCta={footerCta}
  translationSlugEs="/es/legal/terms-of-service"
>
  <InnerHero content={heroContent} />
  <article class="site-container--small mx-auto px-8 prose pb-base">
    <Content />
  </article>
</Layout>
