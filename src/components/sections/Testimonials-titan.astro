---
import { Image } from "astro:assets";
// Define a relaxed testimonial shape compatible with both EN/ES data
// and with avatars coming from either Astro assets or public paths
type TestimonialLite = Readonly<{
  content: string;
  author: string;
  position?: string;
  company?: string;
  avatar: any; // ImageMetadata | string
  status?: string; // allow optional status like "draft"
}>;
import Eyebrow from "../ui/Eyebrow.astro";
import {
  getBackgroundColor,
  getTextColor,
  getPaddingClass,
} from "@utils/styleUtils";
import type { ThemeColor, PaddingSize } from "@utils/styleUtils";

export interface Props {
  content: Readonly<{
    eyebrow?: string;
    title?: string;
    description?: string;
    testimonials: TestimonialLite[];
    cta?: Readonly<{
      heading: string;
      description?: string;
      text: string;
      link: string;
      variant?: "primary" | "secondary" | "ghostLight" | "ghostDark";
      target?: string;
      rel?: string;
    }>;
  }>;
  background?: ThemeColor;
  padding?: PaddingSize;
  paddingTop?: PaddingSize;
  paddingBottom?: PaddingSize;
}

const {
  content: { eyebrow, title, description, cta, testimonials },
  background = "light",
  padding,
  paddingTop,
  paddingBottom,
} = Astro.props;

const bgColor = getBackgroundColor(background);
const textColor = getTextColor(background);
const paddingClass = getPaddingClass({ padding, paddingTop, paddingBottom });
---

<section
  id="testimonials"
  class:list={[bgColor, paddingClass]}
  style="padding-bottom:0 !important;"
>
  <div class="site-container text-center px-4 sm:px-6 md:px-8">
    {eyebrow && <Eyebrow text={eyebrow} background={background} />}

    {title && <h2 class:list={`fade-in-up ${textColor} text-h2 mb-4`}>{title}</h2>}

    {
      description && (
        <p
          class:list={`fade-in-up delay-1 ${textColor} text-base opacity-90 mb-12 max-w-2xl mx-auto`}
        >
          {description}
        </p>
      )
    }

    <div class="grid gap-8 sm:grid-cols-1 md:grid-cols-2">
      {
        testimonials
          .filter(
            (testimonial) =>
              !("status" in testimonial) || testimonial.status !== "draft",
          )
          .map((testimonial, index) => (
            <div
              class:list={`fade-in-up${index > 0 ? ` delay-${(index % 3) + 1}` : ""} bg-white rounded-xl p-6 shadow-md hover:shadow-lg transition-shadow border border-secondary-200 dark:bg-secondary-900 dark:border-secondary-800 text-left`}
            >
              <div class="mb-4 text-yellow-500 text-xl">★★★★★</div>

              <blockquote class="mb-6 italic text-[--color-body-base]">
                "<span set:html={testimonial.content} />"
              </blockquote>

              <div class="flex items-center">
                {typeof testimonial.avatar === "object" ? (
                  <Image
                    src={testimonial.avatar}
                    alt={testimonial.author}
                    class="w-12 h-12 rounded-full mr-4 object-cover"
                    width={48}
                    height={48}
                    loading="lazy"
                  />
                ) : (
                  <img
                    src={testimonial.avatar as any}
                    alt={testimonial.author}
                    class="w-12 h-12 rounded-full mr-4 object-cover"
                    width="48"
                    height="48"
                    loading="lazy"
                    decoding="async"
                  />
                )}
                <div>
                  <p class="font-semibold text-[--color-headline]">
                    {testimonial.author}
                  </p>
                  <p class="text-sm text-[--color-body-light]">
                    {testimonial.position}, {testimonial.company}
                  </p>
                </div>
              </div>
            </div>
          ))
      }
    </div>

    {
      cta && (
        <div class="mt-16 md:mt-24 mb-16 md:mb-24 rounded-2xl bg-background-dark border border-white/10 shadow-xl px-8 py-12 sm:px-12 sm:py-14 md:px-16 md:py-16 lg:px-20 lg:py-20">
          <div class="flex flex-col gap-10 text-center lg:flex-row lg:items-center lg:justify-between lg:gap-16 lg:text-left">
            <div class="max-w-2xl">
              <h3 class="text-2xl md:text-3xl font-bold text-white leading-snug mb-4">
                {cta.heading}
              </h3>
              {cta.description && (
                <p class="text-base sm:text-lg leading-relaxed text-gray-300">
                  {cta.description}
                </p>
              )}
            </div>
            <div class="lg:shrink-0">
              <a
                href={cta.link}
                target={cta.target}
                rel={cta.target === "_blank" ? "noopener noreferrer" : cta.rel}
                class="inline-flex w-full items-center justify-center rounded-lg bg-amber-500 px-8 py-4 text-base font-bold text-gray-950 transition hover:bg-amber-400 sm:w-auto"
              >
                {cta.text}
              </a>
            </div>
          </div>
        </div>
      )
    }
  </div>
</section>
